services:
  app:
    build: .
    ports:
      - "8080:80"
    environment:
      ADMIN_USERNAME: admin
      ADMIN_PASSWORD: changeme
    volumes:
      - sqlite_data:/var/www/html/data

volumes:
  sqlite_data:
